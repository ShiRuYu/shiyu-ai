CREATE CACHED TABLE IF NOT EXISTS "PUBLIC"."AI_EVAL_DATASET" (
    "ID" CHARACTER VARYING(64) NOT NULL,
    "TENANT_ID" BIGINT NOT NULL,
    "OWNER_USER_ID" BIGINT NOT NULL,
    "NAME" CHARACTER VARYING(255) NOT NULL,
    "DESCRIPTION" CHARACTER LARGE OBJECT,
    "CREATED_AT" TIMESTAMP NOT NULL,
    CONSTRAINT "PK_AI_EVAL_DATASET" PRIMARY KEY ("ID")
);
CREATE CACHED TABLE IF NOT EXISTS "PUBLIC"."AI_EVAL_CASE" (
    "ID" CHARACTER VARYING(64) NOT NULL,
    "DATASET_ID" CHARACTER VARYING(64) NOT NULL,
    "TENANT_ID" BIGINT NOT NULL,
    "INPUT_TEXT" CHARACTER LARGE OBJECT NOT NULL,
    "EXPECTED_TEXT" CHARACTER LARGE OBJECT,
    "METADATA_JSON" CHARACTER LARGE OBJECT,
    "CREATED_AT" TIMESTAMP NOT NULL,
    CONSTRAINT "PK_AI_EVAL_CASE" PRIMARY KEY ("ID")
);
CREATE INDEX IF NOT EXISTS "IDX_AI_EVAL_CASE_DATASET" ON "PUBLIC"."AI_EVAL_CASE"("TENANT_ID", "DATASET_ID");
CREATE CACHED TABLE IF NOT EXISTS "PUBLIC"."AI_EVAL_RUN" (
    "ID" CHARACTER VARYING(64) NOT NULL,
    "DATASET_ID" CHARACTER VARYING(64) NOT NULL,
    "TENANT_ID" BIGINT NOT NULL,
    "OWNER_USER_ID" BIGINT NOT NULL,
    "APP_VERSION_ID" CHARACTER VARYING(64),
    "METRIC" CHARACTER VARYING(48) NOT NULL,
    "STATUS" CHARACTER VARYING(24) NOT NULL,
    "PASS_RATE" DOUBLE PRECISION NOT NULL DEFAULT 0,
    "RESULTS_JSON" CHARACTER LARGE OBJECT,
    "CREATED_AT" TIMESTAMP NOT NULL,
    "COMPLETED_AT" TIMESTAMP,
    CONSTRAINT "PK_AI_EVAL_RUN" PRIMARY KEY ("ID")
);

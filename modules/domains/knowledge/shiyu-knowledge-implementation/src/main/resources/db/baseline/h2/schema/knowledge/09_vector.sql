-- Final H2 schema baseline. Generated from the validated schema; do not add migrations here.

CREATE CACHED TABLE "PUBLIC"."VECTOR_KNOWLEDGE_CHUNK"(
    "ID" BIGINT GENERATED BY DEFAULT AS IDENTITY DEFAULT ON NULL NOT NULL,
    "DOCUMENT_ID" BIGINT NOT NULL,
    "CONTENT" CHARACTER VARYING NOT NULL,
    "EMBEDDING" CHARACTER VARYING DEFAULT NULL COMMENT 'JSON float array',
    "METADATA" CHARACTER VARYING DEFAULT NULL COMMENT 'JSON map',
    "CHUNK_INDEX" INTEGER DEFAULT 0,
    "TENANT_ID" BIGINT NOT NULL,
    "CREATE_BY" CHARACTER VARYING(64) DEFAULT NULL,
    "CREATE_TIME" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    "UPDATE_BY" CHARACTER VARYING(64) DEFAULT NULL,
    "UPDATE_TIME" TIMESTAMP DEFAULT NULL,
    "STATUS" TINYINT DEFAULT 1,
    "DEL_FLAG" TINYINT DEFAULT 0,
    "SPACE_ID" BIGINT DEFAULT NULL,
    "VERSION_ID" BIGINT DEFAULT NULL,
    "EMBEDDING_BINARY" BINARY LARGE OBJECT DEFAULT NULL,
    "EMBEDDING_MODEL" CHARACTER VARYING(100) DEFAULT NULL,
    "EMBEDDING_DIMENSION" INTEGER DEFAULT NULL,
    "PAGE_NUMBER" INTEGER DEFAULT NULL,
    "SECTION_PATH" CHARACTER VARYING(1000) DEFAULT NULL,
    "START_OFFSET" INTEGER DEFAULT NULL,
    "END_OFFSET" INTEGER DEFAULT NULL,
    "TOKEN_COUNT" INTEGER DEFAULT NULL,
    CONSTRAINT "PK_VECTOR_KNOWLEDGE_CHUNK" PRIMARY KEY("ID")
);

CREATE INDEX "PUBLIC"."IDX_KC_DOCUMENT" ON "PUBLIC"."VECTOR_KNOWLEDGE_CHUNK"("DOCUMENT_ID" NULLS FIRST);

CREATE INDEX "PUBLIC"."IDX_KNOWLEDGE_CHUNK_SPACE" ON "PUBLIC"."VECTOR_KNOWLEDGE_CHUNK"("TENANT_ID" NULLS FIRST, "SPACE_ID" NULLS FIRST, "DOCUMENT_ID" NULLS FIRST);

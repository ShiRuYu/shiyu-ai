package com.shiyu.ai.agent.contract.runtime;

/**
 * 定义 AI 运行 Source 可用的枚举值及其业务语义。
 */
public enum AiRunSource {
    CONVERSATION,
    GENERATION,
    AGENT,
    KNOWLEDGE,
    MEMORY,
    TOOL,
    API
}

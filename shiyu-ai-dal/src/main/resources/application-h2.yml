spring:
  h2:
    console:
      enabled: true
      path: /h2
      settings:
        web-allow-others: false

mybatis-flex:
  datasource:
    agent:
      type: druid
      driverClassName: org.h2.Driver
      url: jdbc:h2:file:${app.home}/data/h2/shiyudb;MODE=MySQL;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE
      username: sa
      password:
      validationQuery: SELECT 1
# 安全与租户隔离审计

审计基于 2026-09-11 当前源码、H2/外部数据库基线和独立运行实例，覆盖认证、权限、租户、文件、跨域、数据库、Redis、事件与运维入口。结论是：核心请求链路符合前后端逻辑，基础设施 provider 已能独立切换；生产部署仍需按本文的加固清单配置。

## 请求安全链路

1. `SaServletFilter` 对除公开路径外的全部请求执行登录校验。
2. `UserContextInterceptor` 根据 token 加载用户、当前租户、角色和权限上下文。
3. `SaInterceptor` 执行 `@SaCheckPermission` 等方法级授权。
4. 认证结果进入 `ActorContext`，并在请求/编排入口绑定唯一的 `TenantScope`。
5. MyBatis-Flex 租户工厂向租户表查询追加租户条件；租户实体由 `TenantConsistencyListener` 校验归属一致性。
6. 领域服务继续执行对象归属和状态校验；JDBC、自定义 SQL、文件和索引使用同一可信租户来源，不能假定自动获得 Flex 过滤。

前端的菜单隐藏只改善体验，不构成安全边界；最终授权始终以后端为准。

## 本轮已修复

| 项目 | 原问题 | 修复与验证 |
| --- | --- | --- |
| Token 轮换 | `/api/iam/auth/refresh` 被登录过滤器提前拦截，无法只凭请求体中的旧 token 轮换 | 将新路径加入两处公开清单，控制器自行验证 `accessToken`；独立实例验证新 token 有效、旧 token 401 |
| CORS | `allowCredentials=true` 同时允许任意来源模式 | 默认白名单限定 localhost/127.0.0.1 的 5888、5173，可用 `shiyu.web.cors.allowed-origin-patterns` 覆盖；允许来源 200、恶意来源预检 403 |
| 文件写操作 | 上传、删除只有登录和租户目录校验 | 分别增加 `file:upload`、`file:delete` 权限；原始基线已向 super/admin 角色分配这两个权限 |
| 编译边界 | JDK 25 构建时可能误用高于 Java 21 的 API | Maven 编译器改用 `release 21` |

## 当前租户模型（2026-09-12 源码核查）

系统支持租户树，但每个业务请求只绑定一个当前租户。租户树用于管理范围和切换授权，不会让普通业务查询自动包含全部子租户数据。

| 概念 | 当前实现 |
| --- | --- |
| 归属租户 `homeTenantId` | 用户持久化上下文中的归属及委派授权根；缺失时部分流程从有效租户角色分配推导 |
| 当前租户 `currentTenantId` | 本次业务操作的数据作用域，转换成强类型 `TenantId` |
| 当前角色 `currentRole` | 当前租户内选中的角色；必须校验角色、分配和租户的有效状态 |
| 普通切换 `NORMAL` | 目标租户有效，用户拥有该租户的有效角色分配；优先保留适用角色，否则按角色 ID 选择 |
| 委派切换 `PARENT_SUPER_ADMIN` | 归属租户拥有有效超级管理员角色，可在允许的子树内进入没有直接角色分配的子租户，使用目标租户超级管理员角色 |

`AuthIdentityUseCase` 在父租户超级管理员已离开归属租户时，以当前租户作为继续下钻的子树根；允许返回归属租户，不允许直接横跳到该子树之外。目标有直接角色分配时使用普通切换模式。不能把任意管理员标志理解为跨租户数据过滤豁免。

切换成功前先写入用户 `ext_info` 的当前租户、归属租户、角色和切换模式；委派模式记录 `switchFromTenantId`，普通模式移除该字段。持久化失败返回失败；成功后清除用户上下文 Session 和路由菜单缓存。`UserContextInterceptor` 对缓存上下文继续校验运行期状态，失效时重新加载持久化选择，不永久信任旧 Session。选择保存在用户记录上，不应向调用者承诺不同设备之间的切换完全独立。

租户管理服务限制普通管理范围为当前租户子树，并防止非法父节点调整；删除时拒绝删除 ID 为 1 的租户及当前租户。服务中存在 `platformAdmin && homeTenantId == null` 的全局管理分支，但 HTTP 上下文是否能进入该分支需沿认证入口判断，不能仅凭此条件推断普通管理员可访问全平台。

## 统一租户隔离方案（2026-09-12）

本项目采用 **Flex 主导自动隔离 + 业务授权与一致性校验 + 非 Flex 存储显式隔离**。统一的是租户来源和访问规则，不把 JDBC、文件或向量索引强行迁移到 Flex，也不升级当前 MyBatis-Flex 版本（当前为 1.11.7）。

### Flex 1.11.7 的实际行为

官网多租户文档描述了租户字段填充，但本仓库锁定的 1.11.7 源码和本地测试表明：插入时已有非空租户值会保留，只有租户值为空时才由框架补齐；实体必须使用 `@Column(tenantId = true)` 才进入该路径。因而不能把“Flex 会自动覆盖租户值”当作安全边界。`TenantConsistencyListener` 在标准 Mapper 的插入、更新监听点拒绝无作用域实体、跨作用域实体和更新租户归属；审计监听器仍只负责审计字段。

### 统一规则

| 场景 | 规则 |
| --- | --- |
| 普通请求 | 服务端认证产生 `ActorContext`，绑定唯一 `TenantScope`；缺少作用域的普通持久化失败。 |
| Flex 查询、更新、删除 | 由框架生成租户条件；仓储显式 `TenantId` 参数必须与当前 `TenantScope` 一致。 |
| Flex 新增 | 空租户由 Flex 补齐，但监听器要求最终实体有租户且与作用域一致；非空跨租户值直接拒绝。 |
| 单目标跨租户操作 | 先由 IAM/业务服务授权，再通过 `TenantScope.withTenant` 在目标租户作用域执行，退出时恢复原作用域。 |
| JDBC、原生 SQL、文件、向量索引 | 使用可信租户来源，保留参数化租户条件或命名空间校验；不依赖实体监听器。 |
| 全平台、认证和系统扫描 | 使用用途明确的受控入口；`TenantQueryExecutor` 保存、关闭并恢复 Flex 豁免状态，禁止业务层直接关闭过滤。 |

`TenantQueryExecutor` 已覆盖嵌套调用、原有忽略状态和异常退出。认证前身份查找、租户树管理、后台任务发现、备份扫描和平台统计必须分别命名和审计，不能用一个任意 `withoutTenantCondition` 回调代替授权。异步任务必须携带可信租户，在工作线程绑定并清理；现有 `TaskContext` 装饰器不等同于租户传播。

### 租户用量与平台用量

现有 `/api/governance/usage/**` 有意收窄为当前租户统计。手写统计 SQL 均显式接收并使用租户参数，并由真实 H2 MyBatis Mapper 测试验证双租户结果隔离。平台汇总使用独立的 `/api/governance/platform/usage/**` Controller、服务和只读仓储，不提供客户端 `allTenants` 开关。

平台入口除 `platform:usage:read` 外，还要求归属租户和当前租户均为默认租户 ID 1、当前有效角色为 `super`、未处于 `PARENT_SUPER_ADMIN` 委派状态，并由 IAM 契约完成服务端身份检查。默认 H2 种子只向默认租户超级管理员授予该权限；普通租户即使写入同名权限也不能通过身份边界。拒绝访问保留权限错误，不被统计接口的通用异常捕获改写。

## 租户隔离

- 数据库主要业务表通过租户字段和 MyBatis-Flex租户条件隔离。
- 文件 key 固定以 `tenant/{tenantId}/` 开头；列表按命名空间查询，下载和删除会验证前缀。
- `ContextTenantFactory` 始终通过 `TenantScope.require()` 返回唯一租户；缺少上下文会失败，没有按表名返回空租户的白名单。未配置租户列的全局表不依赖该过滤器。认证查找、租户树和部分角色管理仓储现在通过 `TenantQueryExecutor` 使用受控豁免，并保留调用方授权与显式查询条件；架构扫描器禁止其他位置直接调用 `TenantManager.withoutTenantCondition`。
- 线程池、定时任务、消息消费等脱离 HTTP 请求的代码必须显式设置并在 `finally` 清理用户/租户上下文；不得依赖上一个线程残留。
- 新建表时必须决定其是全局表还是租户表，配置租户列及显式授权条件，同步数据字典与隔离测试；禁止增加缺失上下文时放行的隐式回退。

请求中由服务端认证结果构建 `ActorContext` 并绑定 `TenantScope`，MyBatis 对受管理的租户列追加过滤，领域服务继续验证对象归属和操作权限。`ActorContext.requireTenant` 要求租户相等；自定义 SQL、JDBC、索引及对象存储不能假定自动享有 MyBatis 的过滤效果。客户端提交的目标租户只能作为切换或管理请求参数，不能直接成为可信业务上下文。

HTTP 请求结束及上下文加载失败时清理线程上下文。`TenantScope.withTenant` 在 `finally` 恢复原租户。线程任务装饰器恢复的是独立的 `TaskContext` 快照，并在结束时恢复工作线程原快照；这不等同于自动绑定 `TenantScope` 或 `UserContextHolder`。新增异步、调度和消费链路必须显式传递所需上下文并测试清理，不能仅因使用线程装饰器就认定租户上下文已传播。

## 租户默认数据初始化

IAM 的注册、租户创建及相关切换入口通过 `KnowledgeTenantProvisioning` 契约调用知识模块。知识实现按目标租户和默认空间编码查找，已存在则返回，否则创建“企业通用知识空间”，使用通用领域、`TENANT` 访问模式和 `DIRECT` 审核模式。它初始化的是知识空间，不是教育课程、学生或全部平台数据。

当前实现采用事务内先查后建；这不能单独证明并发初始化幂等，仍需结合数据库唯一约束和重复冲突处理验证。初始化是否与入口操作整体原子提交应按各入口事务边界确认，不将其描述为跨模块分布式事务。

业务模块启用采用两层规则：单一 `shiyu-ai-bootstrap` 通过 `shiyu.modules.<module-id>.enabled` 决定实现是否装配；装配后，IAM 的 `AUTH_TENANT_MODULE` 再决定当前租户是否可访问该模块。`TenantModuleAccessService` 从已装配的 `BusinessModuleDescriptor` 初始化新租户默认记录，已有禁用状态不会被覆盖；缺少租户模块记录按拒绝处理。关闭进程模块只停止 Bean、路由和初始化，不删除历史业务表或数据。未来课程、企业、工作流等模块复用该协议，不创建第二个 application。

## 实现状态与待完善项

### 2026-09-14 验收复核

统一实现已通过本仓库可运行的全仓 Maven 测试、严格编译和架构/文档门禁；外部数据库方言、容器依赖和并发场景仍按下列范围标记为未运行，不能把这些未运行环境表述为已验证。

直接声明 `tenantId` 且带 `@Table` 的生产对象复核已为 `TenantMenuDO`、`TenantAuthCodeDO`、`AgentCheckpointDO`、`AuditLogDO`、`ExecutionTimelineDO` 补齐 `@Column(tenantId = true)`；IAM 授权关联表有真实 Mapper 双租户回归，Agent 持久化测试覆盖作用域缺失与参数一致性。此结果不覆盖继承字段或所有原生 SQL，不能替代完整租户表清单。

- 已定位并修正平台统计服务与普通统计服务的 Spring 类型注入冲突，新增容器回归测试；平台仓储契约改为只读，适配器通过组合复用统计计算。
- 认证用例 `AuthAuthenticationUseCase.completeLogin` 已移除整段过滤豁免；用户有效角色及租户分配查询在仓储内临时豁免，并要求有效用户 ID。新增测试检查登录持久化时过滤保持开启、角色查询异常退出后恢复过滤。其余身份查询仍需与真实数据库登录流程一并验收。
- 新用户默认角色分配已在注册编排入口绑定租户 1，测试验证分配发生在默认租户且恢复调用方作用域；文件元数据普通入口已强制要求作用域，过期上传清理和文件迁移任务按记录或文件标识的租户绑定后更新元数据。
- 子租户创建已将事务注解移至公开服务入口，并拆分基础记录保存与安全数据初始化；服务在生成租户标识后绑定新租户作用域，仓储初始化入口严格校验目标租户。定向测试验证公开事务属性、目标作用域及调用方作用域恢复；真实数据库的整条创建事务回滚仍需验证。
- 普通知识、会话、Agent、Memory、Outbox 等 JDBC 持久化入口已改用 `TenantScope.requireMatches`，无作用域或参数不一致会拒绝；`requireMatchesIfBound` 仅保留在 IAM 认证前身份查找、租户树/授权范围读取等明确例外，必须由调用方授权并保留显式租户条件，不能复制到普通业务仓储。
- `TenantMapperIsolationTest` 已使用真实 Flex 1.11.7 Mapper、生产监听器配置和 H2，验证无作用域拒绝、空租户填充、跨租户新增拒绝、混合租户批量写入不落库、更新归属拒绝、按 ID 查询/更新/删除隔离及分页隔离。治理 H2 测试证明概览 SQL 的显式租户条件；关联查询和各领域端到端流程仍需补齐。
- 平台授权已实时校验租户、super 角色、用户有效分配及专用权限，不再以 `platformAdmin` 标志为授权依据；单元测试覆盖停用租户、停用角色、失效或删除的分配以及缺少权限。平台权限除 H2 baseline 种子外，提供 `modules/domains/iam/shiyu-iam-implementation/src/main/resources/db/updates/iam/20260914_platform_usage.sql` 增补脚本；已有 H2 v4 数据库启动时在事务及基线行锁内执行。脚本仅插入缺失项，不覆盖已有禁用或删除标志；物理删除的缺失授权会在下次启动时补齐，撤销权限应使用禁用状态。外部数据库仍由部署方在维护窗口备份后、事务内执行脚本，不在应用启动时自动写入；MySQL/PostgreSQL 尚未实测。真实数据库授权集成验证仍需补齐。
- 门禁已覆盖直接 `ignoreTenantCondition`、关闭过滤方法与工厂注册的静态导入、全限定名和多接口中的额外工厂实现，并按完整基础设施路径识别受控入口，避免同名类绕过。`domain` 包直接调用或静态导入 `TenantScope.set/withTenant/clear` 被禁止；其他编排/仓储包的作用域用途与原生 SQL 豁免清单仍需逐项审计。
- MySQL/PostgreSQL 和容器相关验证尚未完成；本地没有 Docker，因此 Testcontainers 用例显示为跳过。上述属于环境覆盖限制，不改变已通过的源码门禁和 H2/真实 Mapper 验证结论。

已实现：唯一 `TenantScope` 来源、Flex 租户实体写入/更新一致性监听器、受控跨租户执行器、治理当前租户与平台统计分离、平台统计专用权限、关键 JDBC/异步路径的作用域校验，以及 H2 + 真实 MyBatis Mapper 的双租户查询验证。

本次单应用收敛已实现：删除 `shiyu-platform-bootstrap`，由 `shiyu-ai-bootstrap` 作为唯一可执行入口；平台 composition 使用显式包边界；教育实现通过自动配置和进程/租户两层开关接入。模块访问拦截器在认证绑定 `TenantScope` 后、方法级权限校验前执行；模块未装配、租户未启用或缺少作用域时均不放行。

开发约束：新增租户表必须声明 `@Column(tenantId = true)` 或明确列为全局表；普通仓储不得根据入参自行切换作用域；原生 SQL/JDBC、文件和索引必须进入显式隔离清单；跨租户只能在授权编排入口使用受控作用域；缺少作用域不得以空数组或空条件默认放行。

自动检查能力：`scripts/architecture/check_tenant_isolation.py` 输出生产/测试 Java 文件的职责、租户映射和原生 SQL 清单，并阻断未受控的过滤豁免及额外 `TenantFactory`。扫描通过只说明入口约束满足，不等于每条 SQL、文件 key 或向量命名空间已被业务测试证明正确。

尚未完全验证：所有模块的原生 SQL/JDBC/文件/向量存储逐项双租户测试、MySQL/PostgreSQL 方言执行、并发租户初始化和真实异步线程池复用场景；本轮没有升级 Flex、迁移已有业务数据或重写全量注释。模块开关已覆盖静态边界、服务端租户判断和单元测试，但教育关闭后的完整启动上下文及每个未来业务的数据库基线仍需在对应模块接入时验证。文首历史运行审计也不代表本次重新执行了全部部署或多设备联调。

## 公开与半公开路径

无需已有登录态：登录、注册、验证码登录、忘记密码、令牌轮换、验证码、OpenAPI/Swagger、H2 控制台、部分资源路径。注意：刷新接口虽然在登录过滤器中公开，但会在业务层验证旧 token，并非匿名获取 token。

生产建议：

- 关闭 Swagger/OpenAPI 或置于网关/VPN 后；H2 控制台必须关闭。
- 注册、忘记密码和验证码接口增加频率限制、失败计数与审计。
- 不要把 H2 文件、备份、日志、模型密钥目录暴露为静态资源。
- `education-resources` 的实际 URL 与 WebMvc 前缀应通过集成测试固定，避免公开清单和真实路由漂移。

## 密钥、模型与日志

- 模型/API 密钥只通过环境变量或密钥服务注入，不提交到仓库、前端构建物或文档示例。
- 日志不得记录 token、密码、验证码、完整模型密钥和敏感学习内容。
- 文件名、MIME 与大小均视为不可信输入；生产环境应增加恶意文件扫描与可执行内容隔离。
- S3/MinIO/OSS/COS 使用最小权限凭证，bucket 默认私有，下载优先短时签名 URL。
- PostgreSQL/MySQL 连接只从环境变量或密钥管理系统注入；应用启动校验 baseline 版本、种子和表集合，不以空库替换已有数据。
- Redis 只保存租约、限流、幂等键和短期缓存，不保存业务事实；生产连接失败时锁、限流和幂等快速失败，禁止静默回退到跨实例不一致的内存状态。
- Kafka 仅在显式 provider 下创建连接；outbox 消息限制重试次数并进入死信主题，消费端使用事件 inbox 主键去重，凭据和主题配置不得写入日志。

## 上线前强制清单

- [ ] 修改默认管理员密码，停用无用账号和角色。
- [ ] 设置明确的生产 CORS 白名单，启用 HTTPS。
- [ ] 关闭 H2 Console、Swagger UI 和不需要的注册入口。
- [ ] 为登录、验证码、AI 流式请求和上传配置网关限流。
- [ ] 验证跨租户 ID、文件 key、分页查询均不能读取其他租户数据。
- [ ] 验证备份可校验、可异机恢复，且备份文件加密并限制访问。
- [ ] 对依赖执行 SCA/漏洞扫描，并对高危结果完成处置。

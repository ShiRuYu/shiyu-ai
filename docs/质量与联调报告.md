# 代码质量与前后端联调报告

审计时间：2026-08-28。范围：`shiyu-ai` 全模块、`shiyu-ui/apps/web-naive`、OpenAPI 契约、领域架构门禁和核心登录后流程。

## 结论

当前前后端领域切片结构、静态检查、单元测试、覆盖率门禁、生产构建、OpenAPI 合约和本地真实账号驱动的 Playwright E2E 均通过。CI 仍需部署 URL 和受控测试凭据才能启用同一套 E2E 门禁。

## 验证结果

| 范围 | 命令/方式 | 结果 |
| --- | --- | --- |
| 后端严格构建与测试 | `mvn -Pstrict-warnings verify -Ddependency-check.skip=true` | 通过，warnings-as-errors 生效；代码/架构/测试门禁全部通过 |
| 后端领域覆盖率 | `python scripts/architecture/check_domain_coverage.py` | 10 个领域均达到行 ≥90%、分支 ≥80% |
| 架构/Schema/路由 | 三项 `scripts/architecture` 门禁 | 通过；无跨领域 implementation 依赖、Schema 归属有效、无旧 `/v1` Controller 路由 |
| 文档 | `python scripts/docs/verify_documentation.py` | 通过；33 文档、440 operations、97 tables |
| 前端静态门禁 | `pnpm check` | 边界、循环依赖、依赖声明、类型和拼写均通过 |
| 前端单测与覆盖率 | `pnpm test:coverage:features` | 80 文件、501 测试通过；所有 Feature 达到行 ≥90%、分支 ≥80% |
| 前端生产包 | `pnpm build:naive` | 通过，生成 dist.zip |
| OpenAPI/契约 | `pnpm test:openapi`、`pnpm test:contract` | 类型一致；324 次调用、307 个唯一端点、0 未解析表达式 |
| E2E | 本地后端 v4 空库 + Vite + `pnpm test:e2e` | 22 通过、2 按项目设计跳过；CI 需配置 `PLAYWRIGHT_BASE_URL`、`E2E_USERNAME`、`E2E_PASSWORD` |
| 新开发库启动 | `python scripts/verify_fresh_startup.py` | 空 `APP_HOME` 启动成功，`/v3/api-docs` 返回 200，创建 16 个数据文件 |

## 核心联调断言

- 管理员登录成功；返回当前租户、角色和 access token。
- `/api/iam/auth/codes` 返回 131 个权限码，`/api/iam/menus/all` 返回 9 个根菜单、41 个节点。
- 知识空间查询成功。
- `/api/iam/auth/refresh` 使用 `{ "accessToken": "..." }` 成功返回新 token；新 token 可用，旧 token 返回业务码 401。
- localhost 前端来源预检返回 200 和对应 ACAO；未授权外部来源返回 HTTP 403。
- 匿名文件上传返回 401；上传/删除还需分别满足 `file:upload`、`file:delete`。

## 已知非阻断项

- 前端生产构建会输出 `@vueuse/core` 第三方包的无效注解告警，不影响产物。
- CI 原始 Maven 命令还会执行 OWASP Dependency Check；本地未配置 NVD API Key 时更新耗时不可控，CI 应使用缓存或受控的 NVD API Key 完成该扫描，不能将本地跳过参数视为安全扫描通过。
- PowerShell 默认显示部分 UTF-8 中文为乱码；按 UTF-8 实读文件内容正常。源码不存在无效 UTF-8 或替换字符，前端历史 BOM 已清理。
- H2 和本地索引是单实例架构；水平扩展需进行存储层改造。
- 仓库仍有本次破坏性切换的未提交变更；临时 smoke 产物已移出仓库至 `E:\\Temp\\shiyu-smoke-archive-20260826`，最终合入前仍需由集成分支完成审阅和提交。

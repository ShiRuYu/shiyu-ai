# ShiYu AI 开发规范与对话决策

> 本文把项目讨论中已经确认的产品边界、架构决策和交付规则固化为开发约束。代码、H2 基线、OpenAPI 和测试是行为事实源；本文用于解释为什么这样实现以及变更时必须同步什么。
>
> 关联前端规范：[shiyu-ui 开发规范与对话决策](../../shiyu-ui/docs/开发规范与对话决策.md)。

## 1. 已确认的产品方向

ShiYu AI 的定位是：**企业级 Agent/RAG 后端 + Prompt Studio + OpenAI-compatible API + 可插拔聊天前端**。SillyTavern、Dify、Open WebUI、DeepSeek Harness、MAGMA 等项目只作为公开机制的参考，不复制源码、页面或专有实现。

当前默认保持单实例、H2、本地文件、JVector、本地任务调度、disabled Redis 和进程内事件。PostgreSQL/MySQL、Redis、S3/MinIO、Kafka 和 pgvector 已作为可选 provider 接入，但仍需先完成外部设施的基线迁移、密钥注入、租户隔离和回滚演练，不能把空外部存储直接接入生产。

## 2. 对话中形成的架构结论

### 2.1 Conversation 与 Memory 必须分离

- **Conversation** 是原始交互事实源：保存用户实际发送的消息、消息树、编辑、分支、生成和 SSE 续传事实。
- **MAGMA Memory** 是派生认知结构：保存筛选后的事件、实体和 Temporal/Semantic/Causal/Entity 关系图。
- Memory 不复制完整聊天内容；只保存提炼事件和 `sourceType/sourceId` 引用。
- Conversation 不负责长期记忆提取，Memory 不负责流式草稿、取消和生成状态。

### 2.2 Agent 平台与 Agent 执行的关系

- Agent 平台负责定义、版本、图校验、发布和权限治理。
- Agent 执行负责引用已发布版本，创建执行上下文、步骤、工具调用、模型调用和 checkpoint。
- 两者共享 `AiRun`/运行事件，但不把编辑器状态和执行状态混在同一张业务表中。
- 运行时只能调用 Published App/Agent Version；Draft 只能预览、校验或测试。

### 2.3 Prompt Studio 的边界

Prompt Studio 是模板、变量 Schema、版本、预览、对照运行和评测入口，不是意图管理页面。意图路由应归入 Agent/App 的路由配置；Prompt Studio 只消费意图结果或变量，不把意图字段塞进通用 Prompt 表。

### 2.4 Runtime 是跨模块运行事实源

一个用户请求默认只有一个根 `AiRun`。Conversation、Agent、Knowledge、MAGMA、Model、Tool 和 Evaluation 通过类型化事件写入同一运行轨迹；流式恢复使用 `(runId, seq)`，不再维护互相漂移的第二份事件正文。

### 2.5 MAGMA 是平台级记忆基座

Memory 采用独立实现的 MAGMA-based 机制：统一事件节点、四类关系图、向量/关键词/时间联合锚点、意图路由、受限图遍历、快慢双通道和带引用路径的上下文合成。它不是 MAGMA 官方实现，也不依赖 Education 或具体 Agent 类型。

## 3. 后端开发规范

### 3.1 模块依赖

| 模块 | 只负责 | 禁止依赖 |
| --- | --- | --- |
| Conversation | 原始会话、消息树、GenerationRun、SSE | 具体业务域、长期 Memory |
| Runtime | AiRun、运行事件、Trace、usage、审批 | 具体业务实体和供应商请求类型 |
| MAGMA Memory | 事件、实体、边、索引、治理、检索 | 具体业务域、Agent 具体类 |
| Agent | 图、版本、执行、checkpoint | 直接拼接供应商 prompt |
| Knowledge | 文档、切片、权限、检索 | 复制 Memory 事件表 |
| Education 及未来业务域 | 领域适配器和授权 | 修改平台通用表结构来承载领域字段 |

### 模块与 Java 包职责约束（2026-09-12）

- 教育模块位于 `modules/business/education`，平台领域位于 `modules/domains`，公共技术能力位于 `modules/infrastructure`，装配入口位于 `modules/applications`。平台通用能力不得反向依赖教育实现或承载教育专用字段。
- 跨领域通过公开 contract 协作，具体实现由装配层连接；领域 implementation 不作为其他领域的共享 API。`KnowledgeTenantProvisioning` 是 IAM 调用知识初始化的契约示例。
- 按职责拆包：Web Controller、配置、路径贡献者、WebSocket 支撑分别组织；复杂运行包按 model、port、service、adapter、persistence 分离。DTO/request/response 和紧密关联的小型契约聚合可以保留，例外应有审计说明。
- 包迁移同步修改 import、测试包、显式类名、Springdoc 扫描范围和 MVC 路径匹配；验证 HTTP 路由、Bean 名称、数据行为不变。架构约束以扫描器与架构测试共同检查。

### 默认启动与工具链配置（2026-09-15）

- 后端只保留一个可执行应用 `shiyu-ai-bootstrap`。默认端口为 `9000`，默认 Spring profile 为 `dev`，默认数据库为本地 H2，默认事务基础设施 `shiyu.tx.enabled=true`。启动时不应要求开发者额外追加这些参数。
- `SPRING_PROFILES_ACTIVE` 可以覆盖默认 profile；兼容保留 `profiles.active` 旧属性。数据库、文件、向量 provider 和端口均允许通过环境变量或外部配置覆盖，生产环境不得依赖开发 H2 默认值。
- 业务模块不再新增独立 application。模块实现通过自动配置和 `shiyu.modules.<module-id>.enabled` 开关加入同一进程；默认配置位于 `modules/applications/shiyu-ai-bootstrap/src/main/resources/application.yml`，例如 `SHIYU_MODULE_EDUCATION_ENABLED=false` 可关闭教育业务。
- `TransactionTemplateExecutor` 属于平台基础设施，由默认 `shiyu.tx.enabled=true` 保证知识、Agent 等平台服务在直接运行时能够完成 Bean 装配；不得把 `--shiyu.tx.enabled=true` 作为必需的 IDE 运行参数。
- 项目源码要求 JDK 21+，Maven 编译以 `--release 21` 为 API 基线。IntelliJ 的 Project SDK、模块 SDK 和 Maven Runner 必须指向同一个 JDK；`.idea` 属于本地 IDE 状态，不提交到 Git。删除 `.idea` 或重新导入 Maven 项目后，必须重新选择 Project SDK。
- 默认启动配置的回归测试必须验证有效 profile、端口、H2 provider 和事务开关；启动参数只用于临时覆盖，不作为默认行为的替代品。

### 业务模块开关与扩展约定（2026-09-15）

- 一个业务模块由 `contract` 与 `implementation` 组成；实现模块必须提供 `@AutoConfiguration`、`@ConditionalOnBusinessModule`、组件扫描、自动配置 imports 和 `BusinessModuleDescriptor`。平台装配只扫描平台能力，不写入教育或其他具体业务包名。
- 进程级开关只决定模块是否装配：`shiyu.modules.<module-id>.enabled`，环境变量使用 `SHIYU_MODULE_<MODULE_ID>_ENABLED`。关闭后不注册 Controller/Bean、业务路由、模块初始化和模块数据库贡献者；已存在的数据不删除，也不改变历史记录。启动脚本、文档和测试不得创建第二个 application。
- 租户级启用由 IAM 的 `TenantModuleAccessPort` 决定，是进程装配之后的第二道门；请求链路为认证/租户作用域 → 业务模块访问拦截器 → 领域权限校验。它不能绕过进程开关、角色权限或租户隔离。缺少作用域或租户模块记录时默认拒绝，模块迁移和租户创建流程必须显式初始化默认状态。
- 新增业务（例如课程、企业、工作流、插件）复用同一协议，只新增自身目录和自动配置；禁止复制 `application`、扩大根包扫描或把业务字段写入平台表。跨领域通过 contract、事件和明确的组合入口协作。

### Java 注释要求与现状

类和接口注释说明具体职责、服务对象和必要边界；方法注释说明实际操作、关键条件及结果。`@param` 解释参数的业务含义，`@return` 解释真实返回值，必要时说明失败条件。使用合法的 `/** ... */` Javadoc，放在声明及其注解之前，保留原有有价值的行为说明。

“执行某方法定义的接口操作”“方法参数”“操作结果”等无具体含义的模板句不满足功能描述要求。例如知识租户初始化应说明“为指定租户创建默认通用知识空间，已存在时跳过”，不能只复述方法名。

当前 `check_type_javadocs.py` 检查全部 Java 类型是否有类型级 Javadoc，`check_interface_javadocs.py` 检查接口方法，`check_chinese_comments.py` 检查中文注释规则，`check_functional_javadocs.py` 检查已知功能描述模板；这些检查都不能单独证明描述与实现一致。当前功能扫描仍有 3931 条生产历史问题（均在基线中）和 306 条测试观察项；基线策略只禁止新增问题，不代表已有注释已经语义合格。复杂业务注释必须结合实现人工复核，整改按 Agent、Education、IAM、Knowledge、Model 等领域分批完成。

### 3.2 API 路径与 Sa-Token 权限

- 资源 CRUD 使用集合和资源项路径：集合读取/创建使用 `GET/POST /api/{domain}/{resources}`，单项读取/更新/删除使用 `GET/PUT/DELETE /api/{domain}/{resources}/{id}`。旧的 `list/page/detail/create/update/delete` 仅在语义确实是树、选项、执行详情等查询或动作时保留；已迁移的 CRUD 不提供旧别名。
- 状态切换、发布、导入、重建索引等改变业务状态的操作使用明确的动作子路径和 `POST`，不能为了形式统一伪装成普通更新。OpenAPI 快照、前端 facade、API 参考和权限矩阵必须由运行时契约一起更新。
- 接口权限以 Controller 上的 `@SaCheckPermission` 权限码为声明源，由 `SaInterceptor` 统一执行；不要另建按 URL 复制的权限映射表。类级权限会与方法级权限同时生效，写操作应声明最小的创建/编辑/删除权限；若继承类级读取权限会造成额外限制，拆分 Controller 或改为方法级注解。
- Controller 注解只做身份/权限码门禁，Service 必须继续校验租户作用域、资源归属、空间角色和跨租户授权。没有注解的接口必须在安全审计中说明其自服务、公开或内部调用理由，不能把“已登录”当作细粒度授权。
- Agent 的管理资源 CRUD 已使用 `/api/agent/agents` 和 `/{id}`；版本 CRUD 使用嵌套资源 `/api/agent/agents/{agentId}/versions`，版本发布、复制、切换和 Graph 编辑属于独立动作契约，保留动作/兼容入口并在路由清单中标注，不能与管理资源 ID 混用。
- 混合 CRUD Controller 不使用类级读取权限再叠加方法级写权限，避免 Sa-Token 默认的隐式 AND。当前保留类级单一权限的 Controller（教育分析、推荐、错题、资源内容，知识审计/运维/路径，模型网关）每个公开方法都属于同一授权边界；后续增加不同权限的读写方法时必须改为方法级声明并补无权限测试。
- 自服务接口（会话、运行、提示词、工具审批等）可采用“登录 + 当前租户 + 当前用户/资源归属”授权，不要求虚构权限码；平台管理、跨租户和统计接口必须使用显式专用权限并在 Service 再校验身份与租户条件。

### 3.3 API 与数据契约

1. 新接口使用按领域组织的 `/api/{domain}/**` 路径；不恢复旧版本化路径、旧 `/chat/**`、旧 `MemoryService` 或旧 memory 数据兼容层。
2. 模型输入必须是结构化消息和 content parts；平台层禁止把消息降级成单字符串 prompt。
3. 普通写请求使用 `Idempotency-Key` 和数据库唯一约束保证幂等，不能以 Controller JVM 锁替代数据库约束。
4. 客户端不能提交并信任 `tenantId`；服务端必须从认证上下文校验租户、主体、来源对象和权限。
5. 错误必须映射为稳定的 400/403/404/409/422/429/5xx 语义和项目错误码，不直接泄露 `IllegalArgumentException` 或供应商原文。
6. SSE 的 `id` 使用持久化事件 `seq`，支持 `Last-Event-ID`/`afterSeq`、心跳、终态事件和 `[DONE]`。

### 3.4 模型网关

Provider SPI 统一声明 streaming、tools、parallelTools、multimodal、jsonSchema、reasoning、contextWindow、usage 和 cancellation 能力。DeepSeek 等供应商适配器负责原生协议映射、reasoning/text 分流、usage-only frame、并行 tool calls、限流、取消、超时和 malformed frame；平台层只消费统一流事件。

供应商 Key 只能通过受控配置注入，禁止写入源码、前端、日志、Prompt Trace 或运行事件正文。缺失供应商 usage 时才使用本地估算，并明确 `estimated=true`。

### 3.5 H2 与索引

- 当前 v4 是破坏式最终基线；检测到旧基线时拒绝启动，部署方备份后人工重建，程序不迁移或删除旧数据。
- v4 内新增平台统计权限使用 IAM 所有的 `db/updates/iam/20260914_platform_usage.sql` 幂等增补，不重放全量种子、不改业务表结构。已安装 H2 在基线校验后持有基线行锁并在事务内执行；外部数据库由部署方备份后在维护窗口执行并核验，仅 H2 路径已实测。已有权限和授权的禁用状态必须保留，禁止使用重置或覆盖式种子恢复授权；物理删除的缺失记录会被增补，应使用禁用状态表达撤销。
- H2、JVector、关键词索引和 consolidation job 必须能从正式数据重建；切换到 PostgreSQL、S3/MinIO、pgvector、Redis 或 Kafka 时必须保留原 provider 的回退配置和数据。
- 运行数据、构建产物、截图、测试报告和本地密钥不提交到 Git。
- 服务启动时必须遵守数据目录锁，禁止多个进程写同一个 H2/JVector 目录。

### 3.6 租户隔离与 MyBatis-Flex 使用规范

- 当前 MyBatis-Flex 固定为 1.11.7。Flex 是普通租户查询的主隔离机制；`ContextTenantFactory` 只从 `TenantScope.require()` 取得可信租户，不允许按缺失上下文返回空数组放行。
- 租户实体必须使用 `@Column(tenantId = true)`。Flex 1.11.7 会保留已有非空租户值、为空时才补齐，因此 `TenantConsistencyListener` 负责插入/更新的一致性拒绝；审计字段监听器不得承担租户写入职责。
- 普通仓储保留显式 `TenantId` 参数作为契约，必须用 `TenantScope.requireMatches` 校验，不能根据参数自行切换作用域；缺少作用域直接拒绝。`requireMatchesIfBound` 只允许用于认证前身份查找、租户树管理等在隔离清单登记的受控例外。批量写入、更新和删除同样适用。
- 单目标跨租户操作必须先授权，再由编排入口使用 `TenantScope.withTenant`；认证查找、后台发现、备份扫描和平台统计使用 `TenantQueryExecutor` 等用途明确的受控入口。业务层禁止直接调用 `TenantManager.withoutTenantCondition` 或注册额外 `TenantFactory`。
- JDBC、原生 SQL、文件和向量索引不自动享有 Flex 条件，必须使用同一可信租户来源、参数化条件或命名空间校验，并登记在隔离清单中。异步任务显式携带租户并在工作线程清理，不能把 `TaskContext` 装饰器视为租户传播。
- `/api/governance/usage/**` 只返回当前租户；全平台汇总使用 `/api/governance/platform/usage/**`，要求 `platform:usage:read`、归属/当前租户均为 ID 1、有效 `super` 角色且未处于委派状态。客户端不得通过 `allTenants` 开关扩大范围。
- 每个租户路径至少应有：缺少作用域、参数/实体不一致、双租户查询、跨租户授权成功/失败、嵌套作用域和异常清理测试。H2 用于固定框架行为，MySQL/PostgreSQL 用于验证受影响方言；未执行的环境必须标记为待验证。

租户实现状态、Flex 1.11.7 与官网描述的差异、平台用量接口和尚未覆盖的原生 SQL 清单见[安全与租户隔离审计](安全与租户隔离审计.md)。

## 4. 前端全局交互规范

以下规则通过 `@vben/styles/naive` 全局生效，新增页面不得局部覆盖成相反行为：

### 4.1 下拉菜单和完整文本

- 下拉菜单按内容伸展，不默认强制等于触发控件宽度。
- 菜单最大宽度受视口约束；移动端不得产生页面级横向滚动。
- 选择器的选中值保留完整 `title`，鼠标悬浮可以查看全文。
- 选项标签不能因为固定宽度而截断；确需截断时必须提供可访问的完整文本。
- 该规则适用于 Chat、Agent、App Studio、Knowledge、Education、Platform Admin 等所有 Naive UI 选择器，不只修复单个页面。

### 4.2 页面壳与功能归位

- Standard：工作台、知识、列表和管理页面使用全局导航、标题行和内容工具栏。
- Immersive：Chat、学习、答题保持输入区/主要任务可见，内部滚动。
- Builder：Agent、App、Prompt 使用资源栏 + 编辑区 + 属性面板 + 测试/Trace 区。
- Focus：考试、危险确认和完整 Trace 只展示当前任务必要信息。
- 通用 Chat、Agent、RAG 进入统一 AI 控制台；教育只传入场景上下文，不复制一套聊天事实源。

### 4.3 可访问性和响应式

- 可操作目标不小于 44px；图标按钮必须有中文或英文 `aria-label`。
- 首屏必须可看到消息和输入区；Context/Trace 在移动端使用底部面板。
- 页面不得出现横向溢出、无名称按钮和未处理 Promise rejection。
- 文案进入 locale，禁止用业务页面硬编码同义标题制造重复菜单。

## 5. 变更流程与发布门禁

新增或修改一个跨模块能力时，按以下顺序执行：

1. 先更新领域边界和 API/数据契约，再改 Controller、Service、Repository 或前端页面。
2. 同步更新 OpenAPI、H2/外部数据库基线与迁移、权限矩阵、前后端类型和使用文档。
3. 为正常、无权限、跨租户、并发、幂等、取消、重启恢复和移动端布局补测试。
4. 后端执行 `mvn -DskipTests=false test`；前端执行 typecheck、单测、契约检查、构建和 Playwright 关键链路。
5. 发布前确认 Prompt Preview 与真实请求 hash 一致、运行事件可按 seq 重放、上下文带来源/权限、Draft 不可执行。

推荐提交格式：

```text
docs: update development conventions
fix(ui): show complete select labels on hover
feat(conversation): persist generation events
```

## 6. 文档维护规则

文档必须明确区分“源码已实现”“开发必须遵守”和“待验证或待完善”。测试结论记录本次执行范围；严格编译通过不等于全量测试通过，推送成功不等于远端 CI 通过。当前后端 CI 的 push 触发分支为 master、main 和 releases/*，开发分支普通推送不在其中。

提交前审查暂存、未暂存及未跟踪文件，仅纳入开发成果；排除运行数据、密钥、构建输出和临时文件（如 `target-effective-agent`）。检查远端分叉，使用普通提交和推送，推送后核对提交号；不以强推覆盖协作者历史。

- [项目介绍](项目介绍.md)回答“是什么、边界是什么”。
- [技术文档](技术文档.md)回答“模块如何协作、契约如何实现”。
- [使用文档](使用文档.md)回答“开发者和用户如何启动、操作和排错”。
- 本文回答“为什么这样设计、以后如何保持一致”。
- API、数据字典、权限矩阵等生成文档必须由脚本更新后审查 diff，不能手工修改生成结果掩盖代码差异。

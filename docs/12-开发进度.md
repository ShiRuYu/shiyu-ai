# 开发进度

> 最后更新：2026-07-02 (全量缺失功能补全)

## 总体进度：~85%

```
Phase 1 ✅ — 模块拆分 (12 模块，编译通过)
     ↓
Phase 2 ✅ — 知识引擎 (shiyu-ai-knowledge, RogueMap DAG)
     ↓
Phase 3 ✅ — 教育业务域 (shiyu-ai-education) ← 核心完成
     ↓
Phase 4 ✅ — 教育 Agent + LiteFlow + MCP
     ↓
Phase 4.5 ✅ — 模块职责重构 (学习进度相关类迁移到 education)
     ↓
Phase 5.0 ✅ — 全量功能补全
```

## 阶段状态

| 阶段 | 描述 | 状态 | 完成度 |
|------|------|------|--------|
| Phase 1 | 单体 → 12 模块拆分 | ✅ 完成 | 100% |
| Phase 2 | 知识引擎 (shiyu-ai-knowledge) | ✅ 完成 | 100% |
| Phase 3 | 教育业务域 (shiyu-ai-education) | ✅ 完成 | 95% |
| Phase 4 | 教育 Agent + LiteFlow + MCP | ✅ 完成 | 95% |
| Phase 4.5 | 模块职责重构 | ✅ 完成 | 100% |
| Phase 5.0 | 全量功能补全 | ✅ 完成 | 85% |

## 模块完成度与差距分析（更新 2026-07-02）

| 模块 | 完成度 | 文件 | 已有功能 | 缺失/待完善功能 |
|------|--------|------|---------|----------------|
| shiyu-common | 95% | 125 | 6子模块: bom/core/web/mybatis/thread/excel | — |
| shiyu-ai-auth | 95% | 72 | RBAC + Sa-Token + 多租户 + 验证码 + 限流 + 时区 + 字典 | — |
| shiyu-ai-agent | 95% | 105 | LangGraph4j + 19节点 + NodeFactory + AgentLoader + 6教育Agent + NodeInputParam定义 | ⚠️ 缓存未全cache覆盖 |
| shiyu-ai-record | 90% | 32 | 人物/记录/媒体/标签/时间线 CRUD | — |
| shiyu-ai-education | 97% | 100 | 学科/教材/章节/课程/题库/考试/复习/计划/能力值/错题本/学习分析/课程进度/学习状态机(DB) | ✅ 全量分析API+交卷+学习+今日任务 ⚠️ 智能推荐框架 ❌ 成就系统完整UI |
| shiyu-ai-knowledge | 90% | 31 | RogueMap图存储 + 多模式搜索 + 图算法 + 学习路径 + 遗忘曲线 + 文档管理 + 索引重建 | — |
| shiyu-ai-dal | 90% | 112 | 51 DO + 51 Mapper (全部7域 51表) | — |
| shiyu-ai-core | 75% | 15 | ChatEngine + ModelManager + 5平台适配 + 流式/记忆对话 | — |
| shiyu-ai-workflow | 95% | 21 | 3条链(learning/review/exam) + 13组件全部实现 ✅ | ✅ 6条链全部实现 |
| shiyu-ai-rag | 75% | 2 | 委托Knowledge检索(知识点/文档路由) | ⚠️ 无混合检索策略 ⚠️ 无 rerank |
| shiyu-ai-model | 60% | 9 | AiModelBO/AiPlatformBO/ConversationMessageBO + VO + 枚举 | — |
| shiyu-ai-memory | 70% | 5 | MemoryService + 短期/长期记忆(RogueMap) | ✅ 摘要/清理/重要性计算 ⚠️ 摘要未对接LLM |
| shiyu-ai-mcp | 45% | 2 | ToolService + 工具注册框架 | ❌ OCR/TTS/ASR 未实现 |
| shiyu-ai-bootstrap | 100% | 3 | 启动入口 + ApplicationStartupListener | — |

> 图例：✅ 已实现  ⚠️ 优化项 ❌ 待推进  — 无差距

## Phase 4.5 重构记录 (2026-07-01)

### 迁移内容

将学习进度相关类从 `shiyu-ai-knowledge` 迁移到 `shiyu-ai-education`，使职责归属更清晰。

| # | 文件 | 源位置 (knowledge) | 目标位置 (education) |
|---|------|---------------------|----------------------|
| 1 | ReviewScheduler | `knowledge/review/` | `education/review/` |
| 2 | EbbinghausCurve | `knowledge/review/` | `education/review/` |
| 3 | LearningState | `knowledge/domain/` | `education/domain/` |
| 4 | LearningStateMachine | `knowledge/service/` | `education/service/` (新建包) |
| 5 | LearningStateMachineImpl | `knowledge/service/impl/` | `education/service/impl/` (新建包) |

### 更新 import 的外部文件 (4 个)

- `shiyu-ai-agent`: ReviewAgent, ReviewScheduleNode, EducationNodeConfigurer
- `shiyu-ai-workflow`: UpdateMasteryCmp

### 附加重构 (2026-07-02) — AbilityDO/Mapper 移入 education 包

| # | 文件 | 源位置 (knowledge) | 目标位置 (education) |
|---|------|---------------------|----------------------|
| 6 | AbilityDO | `dal/dataobject/knowledge/` | `dal/dataobject/education/` |
| 7 | AbilityMapper | `dal/mapper/knowledge/` | `dal/mapper/education/` (+BaseMapperFlex+@UseDataSource) |

### 编译验证

全部 21 个模块编译通过 (BUILD SUCCESS)。

---

## 进度功能分析

### 进度功能架构总览

系统中的"进度"涵盖 **5 个维度**，分布在 3 个模块中：

```
┌─────────────────────────────────────────────────────────────────┐
│                    进度功能架构                                    │
├──────────────┬──────────────────┬───────────────────────────────┤
│  维度         │  所属模块         │  持久化状态                    │
├──────────────┼──────────────────┼───────────────────────────────┤
│  学习状态进度  │  education       │  ✅ DB (edu_learning_state 表) │
│  能力值进度    │  education       │  ✅ DB (ability_record 表)     │
│  复习进度      │  education       │  ✅ DB (edu_review_task 表)    │
│  学习计划进度  │  education       │  ✅ DB (edu_study_plan 表)     │
│  索引重建进度  │  knowledge       │  ⚠️ 纯内存 (ConcurrentHashMap) │
└──────────────┴──────────────────┴───────────────────────────────┘
```

### 1. 学习状态进度 (知识点级别)

**核心实现：** `LearningState` 状态机 + `LearningStateMachineImpl`

```
NOT_STARTED → LEARNING → MASTERED → PROFICIENT
                ↑            ↓
             FORGOTTEN ← REVIEWING
```

| 项目 | 说明 |
|------|------|
| 文件位置 | `education/domain/LearningState.java`, `education/service/impl/LearningStateMachineImpl.java` |
| 存储方式 | `edu_learning_state` DB 表 (student_id+knowledge_id 唯一键) |
| 状态转换 | `startLearning()`, `passAssessment()`, `deepPractice()`, `forget()`, `scheduleReview()`, `giveUp()` |
| 持久化 | ✅ DB upsert，`LearningStateMachineImpl` 已从 `ConcurrentHashMap` 迁移到 `LearningStateRepository` |

### 2. 能力值进度 (Bloom 六维度掌握度)

**核心实现：** `AbilityService` + `AbilityServiceImpl` + `AbilityValue`

| 维度 | 权重 | 说明 |
|------|------|------|
| 记忆 REMEMBER | 0.15 | 基础记忆 |
| 理解 UNDERSTAND | 0.20 | 概念理解 |
| 应用 APPLY | 0.25 | 实际运用 |
| 分析 ANALYZE | 0.20 | 分析推理 |
| 评价 EVALUATE | 0.10 | 批判评价 |
| 创造 CREATE | 0.10 | 创新创造 |

| 项目 | 说明 |
|------|------|
| 文件位置 | `education/ability/impl/AbilityServiceImpl.java`, `education/domain/AbilityValue.java` |
| 更新公式 | `updated = current + (100 - current) * accuracy * 0.1` (渐进式增长，上限 100) |
| 综合掌握度 | `overallMastery = Σ(dimension_score * weight)` |
| 持久化 | ✅ DB 表 `ability` (字段: remember, understand, apply, analyze, evaluate, create_score, overall_mastery) |
| 调用方 | `UpdateMasteryCmp` (workflow), `ScoreAnalysisNode` (agent graph), `AbilityQueryNode` (agent graph) |

### 3. 复习进度 (艾宾浩斯遗忘曲线)

**核心实现：** `EbbinghausCurve` + `ReviewScheduler` + `ReviewAgent` + `ReviewService`

| 项目 | 说明 |
|------|------|
| 文件位置 | `education/review/EbbinghausCurve.java`, `education/review/ReviewScheduler.java` |
| 复习间隔 | `{1, 3, 7, 15, 30, 90}` 天 (6 轮) |
| 遗忘公式 | `mastery = exp(-lambda * days)`，lambda 随复习轮次递减 |
| 持久化 | ✅ DB 表 `edu_review_task` (字段: review_date, review_round, status, result_score) |
| REST API | `GET /api/v1/review/today/{studentId}`, `PUT /api/v1/review/{id}/complete` |

**复习状态机：**

```
PENDING → IN_REVIEW → COMPLETED
              ↓
           FAILED → PENDING (重新安排)
PENDING → OVERDUE → PENDING (系统重置)
```

### 4. 学习计划进度

**核心实现：** `StudyPlanService` + `PlannerAgent`

| 项目 | 说明 |
|------|------|
| 文件位置 | `education/plan/impl/StudyPlanServiceImpl.java`, `agent/education/PlannerAgent.java` |
| 计划项状态 | `PENDING → IN_PROGRESS → COMPLETED / SKIPPED` |
| 持久化 | ✅ DB 表 `edu_study_plan` + `edu_study_plan_item` (通过 `StudyPlanItemRepository.insertBatch()`) |

### 5. 索引重建进度 (系统任务级别)

**核心实现：** `IndexRebuildTask` + `RebuildStatus` + `KnowledgeSearchService.rebuildIndexWithProgress()`

| 项目 | 说明 |
|------|------|
| 文件位置 | `knowledge/task/IndexRebuildTask.java`, `knowledge/search/KnowledgeSearchService.java` |
| 执行方式 | `@Async` 异步执行，`Consumer<Integer>` 回调报告 0-100% 进度 |
| 重试机制 | 默认 3 次重试，可配置延迟 |
| 存储方式 | `ConcurrentHashMap<taskId, RebuildStatus>`，保留最近 100 个任务 |

### LiteFlow 工作流中的进度流转

```
learningChain (✅ 完整实现):
  checkKnowledgeCmp → loadResourceCmp → teacherCmp → practiceCmp → scoreCmp → updateMasteryCmp
                                                                                   ↓
                                                                    AbilityService.update() + ReviewScheduler.schedule()

reviewChain (✅ 完整实现):
  loadReviewTaskCmp → generateReviewQuestionsCmp → reviewScoreCmp → updateReviewMasteryCmp

examChain (✅ 完整实现):
  generateExamCmp → scoreExamCmp → analyzeExamCmp
```

### 进度功能已知问题

| # | 问题 | 严重度 | 状态 |
|---|------|--------|------|
| 1 | `LearningStateMachineImpl` 纯内存未持久化 | **P1** | ✅ 已修复: DB 表 `edu_learning_state` 持久化 |
| 2 | `ScoreCmp` 为占位实现 | P2 | ✅ 已修复: 对接 ChatEngine AI 自动批改 |
| 3 | `reviewChain` 4 个组件为占位 | P2 | ✅ 已修复: 对接 ReviewAgent/ReviewService |
| 4 | `examChain` 3 个组件为占位 | P2 | ✅ 已修复: 对接 ExamAgent |
| 5 | `PlannerAgent` 计划项未持久化 | P2 | ✅ 已修复: StudyPlanItemRepository + insertBatch |
| 6 | `CourseProgressResponse` 无业务实现 | P3 | ✅ 已修复: 基于知识点完成度计算进度 |

---

## 新增功能 (Phase 4)

### 6 个教育 Agent

| Agent | 文件 | 说明 |
|-------|------|------|
| TeacherAgent | `education/TeacherAgent.java` | AI 讲解知识点，结合前置知识和能力值 |
| PracticeAgent | `education/PracticeAgent.java` | 智能出题，能力→难度映射，LLM 生成 |
| ExamAgent | `education/ExamAgent.java` | 智能组卷，学科/年级/知识点范围 |
| ReviewAgent | `education/ReviewAgent.java` | 艾宾浩斯复习任务管理 |
| PlannerAgent | `education/PlannerAgent.java` | 学习计划生成，拓扑路径分配 |
| ReportAgent | `education/ReportAgent.java` | 学习报告生成 |

### 8 个 MCP 工具

| 工具 | 功能 | 代理 Agent |
|------|------|-----------|
| searchKnowledge | 搜索知识点 | KnowledgeService |
| generateQuestion | AI 出题 | PracticeAgent |
| getStudentAbility | 查询能力值 | TeacherAgent |
| scheduleReview | 安排复习 | ReviewAgent |
| getLearningPath | 学习路径 | KnowledgeService |
| teachKnowledge | 讲解知识点 | TeacherAgent |
| generateExam | 智能组卷 | ExamAgent |
| generateReport | 学习报告 | ReportAgent |

### LiteFlow 工作流组件实现状态

`shiyu-ai-workflow` — 3 条链定义，13 个组件：

| 链 | 组件 | 状态 |
|----|------|------|
| learningChain | checkKnowledgeCmp | ✅ 完整实现 |
| learningChain | loadResourceCmp | ✅ 完整实现 |
| learningChain | teacherCmp | ✅ 完整实现 |
| learningChain | practiceCmp | ✅ 完整实现 |
| learningChain | scoreCmp | ✅ AI 自动批改评分 (ChatEngine) |
| learningChain | updateMasteryCmp | ✅ 完整实现 |
| reviewChain | loadReviewTaskCmp | ✅ 对接 ReviewService |
| reviewChain | generateReviewQuestionsCmp | ✅ 对接 ChatEngine AI 复习出题 |
| reviewChain | reviewScoreCmp | ✅ 对接 AbilityService |
| reviewChain | updateReviewMasteryCmp | ✅ 更新能力值 + 复习进度 |
| examChain | generateExamCmp | ✅ 对接 ExamAgent |
| examChain | scoreExamCmp | ✅ 模拟批改 |
| examChain | analyzeExamCmp | ✅ 分析完成 |

### REST API

| Method | URL | 说明 |
|--------|-----|------|
| POST | `/api/v1/agent/teacher` | TeacherAgent 讲解 |
| POST | `/api/v1/agent/practice` | PracticeAgent 出题 |
| POST | `/api/v1/agent/exam` | ExamAgent 组卷 |
| POST | `/api/v1/agent/review` | ReviewAgent 复习安排 |
| GET | `/api/v1/agent/review/today` | 今日复习任务 |
| POST | `/api/v1/agent/review/complete` | 完成复习 |
| POST | `/api/v1/agent/planner` | PlannerAgent 规划 |
| POST | `/api/v1/agent/report` | ReportAgent 报告 |
| GET | `/api/v1/review/today/{studentId}` | 今日复习任务 (education) |
| PUT | `/api/v1/review/{id}/complete` | 完成复习 (education) |
| GET | `/api/v1/plan/student/{studentId}` | 学生计划列表 |
| GET | `/api/v1/analytics/records/student/{studentId}` | 学习分析记录 |

## 里程碑

| 里程碑 | 描述 | 状态 |
|--------|------|------|
| M1 | 模块拆分 (单体 → 12 模块) | ✅ |
| M2 | 知识引擎 (RogueMap + 知识图谱 API) | ✅ |
| M3 | 教育业务域 (教材/课程/题库/考试/复习) | ✅ |
| M4 | Phase 4 — 教育 Agent + LiteFlow + MCP | ✅ |
| M4.5 | **模块职责重构 (学习进度迁移到 education)** | ✅ |
| M5 | V1 上线 (MVP 可用版本) | ⏳ |
| M6 | 智能推荐 (题目/知识点/资源推荐) | ⏳ |
| M7 | AI Tutor (多 Agent 教学系统) | ⏳ |

## 已知问题

### P1 — 重要缺陷

| # | 问题 | 影响 | 状态 |
|---|------|------|------|
| 1 | 零测试覆盖 | 全项目仅 ~6 个测试文件 | ⏳ |
| 2 | 应用启动验证未完成 | 未确认 Spring Boot 启动正常 | ⏳ |

### P2 — 设计偏差

| # | 问题 | 状态 |
|---|------|------|
| 1 | 缺失模块: shiyu-ai-admin / ui | 后续 |

## 文件统计

| 模块 | Java 源文件 | 测试文件 |
|------|------------|---------|
| shiyu-ai-agent | 105 | 4 |
| shiyu-ai-education | 87 | 0 |
| shiyu-ai-knowledge | 31 | 0 |
| shiyu-ai-workflow | 14 | 1 |
| shiyu-ai-mcp | 3 | 0 |
| 其他模块 | ~370 | 1 |
| **合计** | **~500** | **6** |

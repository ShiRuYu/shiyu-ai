<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shiyu.ai.agent.dal.mapper.auth.UserRoleMapper">

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" resultType="com.shiyu.ai.agent.dal.dataobject.auth.RoleDO">
        SELECT 
            r.id,
            r.code,
            r.name,
            r.status,
            r.del_flag AS delFlag,
            r.create_time AS createTime,
            r.update_time AS updateTime
        FROM role r
        INNER JOIN user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.status = '1'
          AND r.del_flag = 0
        ORDER BY r.id
    </select>

</mapper>

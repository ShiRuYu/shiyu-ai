<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shiyu.ai.agent.dal.mapper.auth.AuthCodeMapper">

    <!-- 根据角色 ID 查询权限码列表 -->
    <select id="selectCodesByRoleId" resultType="java.lang.String">
        SELECT code
        FROM auth_code
        WHERE role_id = #{roleId}
          AND status = '1'
          AND del_flag = 0
        ORDER BY id
    </select>

    <!-- 根据用户名查询权限码列表 -->
    <select id="selectCodesByUsername" resultType="java.lang.String">
        SELECT DISTINCT ac.code
        FROM auth_code ac
        INNER JOIN user_workspace_role uwr ON ac.role_id = uwr.role_id
        INNER JOIN `user` u ON uwr.user_id = u.id
        WHERE u.username = #{username}
          AND ac.status = '1'
          AND ac.del_flag = 0
          AND u.status = '1'
          AND u.del_flag = 0
        ORDER BY ac.id
    </select>
    
    <!-- 根据用户 ID 查询权限码列表 -->
    <select id="selectCodesByUserId" resultType="java.lang.String">
        SELECT DISTINCT ac.code
        FROM auth_code ac
        INNER JOIN user_workspace_role uwr ON ac.role_id = uwr.role_id
        WHERE uwr.user_id = #{userId}
          AND ac.status = '1'
          AND ac.del_flag = 0
        ORDER BY ac.id
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shiyu.ai.agent.dal.mapper.auth.UserMapper">

    <!-- 根据用户名查询用户基本信息 -->
    <select id="selectUserWithRolesByUsername" resultType="com.shiyu.ai.agent.dal.dataobject.auth.UserDO">
        SELECT 
            id,
            username,
            password,
            status,
            del_flag AS delFlag,
            create_time AS createTime,
            update_time AS updateTime,
            nick_name AS nickName,
            gender,
            avatar,
            address,
            email
        FROM `user`
        WHERE username = #{username}
          AND status = '1'
          AND del_flag = 0
    </select>

</mapper>
